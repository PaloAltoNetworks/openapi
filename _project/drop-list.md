# Plane inventory

Generated by `_project/gen_drop_list.py` from `openapi.yaml` and
`_project/planes.yaml`, so it shows what the specification **currently
ships** -- paths already dropped are gone from it. Regenerate after any
change.

> Decisions do not live here. This file is generated and any mark made in it
> is lost on the next run. The durable record of what is not shipped is
> `_project/drops.yaml`, enforced by `scripts/build.py`; the record of which
> plane a path is on is `_project/planes.yaml`. Change those.

**Every path is classified.** The build fails if one is not.

**✓** marks a path a human classified, recorded in `planes.yaml` as coming from `classification.yaml` (23 of 114).
Everything unmarked was read off the base's per-path server overrides before
those were deleted, and is **unverified**.

## Blast radius

Components each group reaches, and how many *only* that group reaches
(those are the ones orphaned if the group is dropped):

| Group | Paths | Components reached | Reached only by this group |
|---|---|---|---|
| control plane | 62 | 195 | **193** |
| gateway | 52 | 276 | **274** |
| unclassified | 0 | 0 | **0** |
| *total* | 114 | 469 | |

Dropping every control-plane path would orphan **193 components**, which must be pruned in the same change rather than left to accumulate.

## Control Plane — 62 paths

Control-plane paths that survive the drops in `drops.yaml`.

### API Keys

- [ ] `/api-keys` — `GET` *(no operationId)*
- [ ] `/api-keys/{id}` — `DELETE` *(no operationId)*, `GET` *(no operationId)*, `PUT` *(no operationId)*
- [ ] `/api-keys/{id}/rotate` — `POST` *(no operationId)*
- [ ] `/api-keys/{type}/{sub-type}` — `POST` *(no operationId)*

### Analytics > Graphs

- [ ] `/analytics/graphs/cache/hit-rate` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/cache/latency` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/cost` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/errors` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/errors/rate` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/errors/stacks` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/errors/status-codes` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/feedbacks` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/feedbacks/ai-models` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/feedbacks/scores` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/feedbacks/weighted` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/latency` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/requests` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/requests/rescued` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/tokens` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/users` — `GET` *(no operationId)*
- [ ] `/analytics/graphs/users/requests` — `GET` *(no operationId)*

### Analytics > Groups

- [ ] `/analytics/groups/ai-models` — `GET` *(no operationId)*
- [ ] `/analytics/groups/metadata/{metadataKey}` — `GET` *(no operationId)*
- [ ] `/analytics/groups/provider` — `GET` *(no operationId)*
- [ ] `/analytics/groups/users` — `GET` *(no operationId)*

### Analytics > Summary

- [ ] `/analytics/summary/cache` — `GET` *(no operationId)*

### Configs

- [ ] `/configs` — `GET` listConfigs, `POST` createConfig
- [ ] `/configs/{slug}` — `DELETE` deleteConfig, `GET` getConfig, `PUT` updateConfig
- [ ] `/configs/{slug}/versions` — `GET` listConfigVersions

### Feedback

- [ ] `/feedback` — `POST` createFeedback
- [ ] `/feedback/{id}` — `PUT` updateFeedback

### Guardrails

- [ ] `/guardrails` ✓ — `GET` listGuardrails, `POST` createGuardrail
- [ ] `/guardrails/{guardrailId}` ✓ — `DELETE` deleteGuardrail, `GET` getGuardrail, `PUT` updateGuardrail
- [ ] `/guardrails/{guardrailId}/mcp-servers` ✓ — `GET` listGuardrailMcpServers, `PUT` bulkSyncGuardrailMcpServers
- [ ] `/guardrails/{guardrailId}/mcp-servers/{mcpServerId}` ✓ — `PUT` upsertGuardrailMcpServer

### Integrations

- [ ] `/integrations` — `GET` *(no operationId)*, `POST` *(no operationId)*
- [ ] `/integrations/{slug}` — `DELETE` *(no operationId)*, `GET` *(no operationId)*, `PUT` *(no operationId)*

### Integrations > Models

- [ ] `/integrations/{slug}/models` ✓ — `DELETE` *(no operationId)*, `GET` *(no operationId)*, `PUT` *(no operationId)*

### Integrations > Workspaces

- [ ] `/integrations/{slug}/workspaces` ✓ — `GET` *(no operationId)*, `PUT` *(no operationId)*

### MCP Integrations

- [ ] `/mcp-integrations` — `GET` McpIntegrations_list, `POST` McpIntegrations_create
- [ ] `/mcp-integrations/{mcpIntegrationId}` — `DELETE` McpIntegrations_delete, `GET` McpIntegrations_retrieve, `PUT` McpIntegrations_update

### MCP Integrations > Capabilities

- [ ] `/mcp-integrations/{mcpIntegrationId}/capabilities` — `GET` McpIntegrationCapabilities_list, `PUT` McpIntegrationCapabilities_bulkUpdate

### MCP Integrations > Metadata

- [ ] `/mcp-integrations/{mcpIntegrationId}/metadata` — `GET` McpIntegrationMetadata_retrieve

### MCP Integrations > Workspaces

- [ ] `/mcp-integrations/{mcpIntegrationId}/workspaces` — `GET` McpIntegrationWorkspaces_list, `PUT` McpIntegrationWorkspaces_bulkUpdate

### MCP Servers

- [ ] `/mcp-servers` — `GET` McpServers_list, `POST` McpServers_create
- [ ] `/mcp-servers/{mcpServerId}` — `DELETE` McpServers_delete, `GET` McpServers_retrieve, `PUT` McpServers_update
- [ ] `/mcp-servers/{mcpServerId}/test` — `POST` McpServers_test

### MCP Servers > Capabilities

- [ ] `/mcp-servers/{mcpServerId}/capabilities` — `GET` McpServerCapabilities_list, `PUT` McpServerCapabilities_bulkUpdate

### MCP Servers > Connections

- [ ] `/mcp-servers/{mcpServerId}/connections` — `DELETE` McpServerConnections_delete, `GET` McpServerConnections_list

### MCP Servers > User Access

- [ ] `/mcp-servers/{mcpServerId}/user-access` — `GET` McpServerUserAccess_list, `PUT` McpServerUserAccess_bulkUpdate

### Model Pricing

- [ ] `/model-configs/pricing/{provider}/{model}` ✓ — `GET` getModelPricing

### Models

- [ ] `/models/{model}` ✓ — `DELETE` deleteModel, `GET` retrieveModel

### Providers

- [ ] `/providers` — `GET` *(no operationId)*, `POST` *(no operationId)*
- [ ] `/providers/{slug}` — `DELETE` *(no operationId)*, `GET` *(no operationId)*, `PUT` *(no operationId)*

### Rate Limit Policies

- [ ] `/policies/rate-limits` ✓ — `GET` listRateLimitsPolicies, `POST` createRateLimitsPolicy
- [ ] `/policies/rate-limits/{rateLimitsPolicyId}` ✓ — `DELETE` deleteRateLimitsPolicy, `GET` getRateLimitsPolicy, `PUT` updateRateLimitsPolicy

### Secret References

- [ ] `/secret-references` — `GET` listSecretReferences, `POST` createSecretReference
- [ ] `/secret-references/{secretReferenceId}` — `DELETE` deleteSecretReference, `GET` getSecretReference, `PUT` updateSecretReference

### Usage Limit Policies

- [ ] `/policies/usage-limits` ✓ — `GET` listUsageLimitsPolicies, `POST` createUsageLimitsPolicy
- [ ] `/policies/usage-limits/{policyUsageLimitsId}` ✓ — `DELETE` deleteUsageLimitsPolicy, `GET` getUsageLimitsPolicy, `PUT` updateUsageLimitsPolicy
- [ ] `/policies/usage-limits/{policyUsageLimitsId}/entities` ✓ — `GET` listUsageLimitsPolicyEntities
- [ ] `/policies/usage-limits/{policyUsageLimitsId}/entities/{entityId}/reset` ✓ — `PUT` resetUsageLimitsPolicyEntity

## Unclassified — 0 paths

None. Every path in the specification has been classified.

## Gateway — 52 paths

Data plane. Listed for completeness; expected to stay.

### Assistants

- [ ] `/assistants` — `GET` listAssistants, `POST` createAssistant
- [ ] `/assistants/{assistant_id}` — `DELETE` deleteAssistant, `GET` getAssistant, `POST` modifyAssistant
- [ ] `/threads` — `POST` createThread
- [ ] `/threads/runs` — `POST` createThreadAndRun
- [ ] `/threads/{thread_id}` — `DELETE` deleteThread, `GET` getThread, `POST` modifyThread
- [ ] `/threads/{thread_id}/messages` — `GET` listMessages, `POST` createMessage
- [ ] `/threads/{thread_id}/messages/{message_id}` — `DELETE` deleteMessage, `GET` getMessage, `POST` modifyMessage
- [ ] `/threads/{thread_id}/runs` — `GET` listRuns, `POST` createRun
- [ ] `/threads/{thread_id}/runs/{run_id}` — `GET` getRun, `POST` modifyRun
- [ ] `/threads/{thread_id}/runs/{run_id}/cancel` — `POST` cancelRun
- [ ] `/threads/{thread_id}/runs/{run_id}/steps` — `GET` listRunSteps
- [ ] `/threads/{thread_id}/runs/{run_id}/steps/{step_id}` — `GET` getRunStep
- [ ] `/threads/{thread_id}/runs/{run_id}/submit_tool_outputs` — `POST` submitToolOuputsToRun

### Audio

- [ ] `/audio/speech` — `POST` createSpeech
- [ ] `/audio/transcriptions` — `POST` createTranscription
- [ ] `/audio/translations` — `POST` createTranslation

### Batch

- [ ] `/batches` — `GET` listBatches, `POST` createBatch
- [ ] `/batches/{batch_id}` — `GET` retrieveBatch
- [ ] `/batches/{batch_id}/cancel` — `POST` cancelBatch
- [ ] `/batches/{batch_id}/output` — `GET` getBatchOutput

### Chat

- [ ] `/chat/completions` — `POST` createChatCompletion

### Completions

- [ ] `/completions` — `POST` createCompletion

### Embeddings

- [ ] `/embeddings` — `POST` createEmbedding

### Files

- [ ] `/files` — `GET` listFiles, `POST` createFile
- [ ] `/files/{file_id}` — `DELETE` deleteFile, `GET` retrieveFile
- [ ] `/files/{file_id}/content` — `GET` downloadFile

### Fine-Tuning

- [ ] `/fine_tuning/jobs` — `GET` listPaginatedFineTuningJobs, `POST` createFineTuningJob
- [ ] `/fine_tuning/jobs/{fine_tuning_job_id}` — `GET` retrieveFineTuningJob
- [ ] `/fine_tuning/jobs/{fine_tuning_job_id}/cancel` — `POST` cancelFineTuningJob
- [ ] `/fine_tuning/jobs/{fine_tuning_job_id}/checkpoints` — `GET` listFineTuningJobCheckpoints
- [ ] `/fine_tuning/jobs/{fine_tuning_job_id}/events` — `GET` listFineTuningEvents

### Images

- [ ] `/images/edits` — `POST` createImageEdit
- [ ] `/images/generations` — `POST` createImage
- [ ] `/images/variations` — `POST` createImageVariation

### Logs

- [ ] `/logs` — `POST` *(no operationId)*
- [ ] `/logs/{logId}` — `GET` *(no operationId)*

### Models

- [ ] `/models` ✓ — `GET` listModels

### Moderations

- [ ] `/moderations` — `POST` createModeration

### OCR

- [ ] `/ocr` — `POST` createOcr

### Realtime

- [ ] `/realtime` — `GET` connectRealtime

### Rerank

- [ ] `/rerank` — `POST` createRerank

### Responses

- [ ] `/responses` — `POST` createResponse
- [ ] `/responses/{response_id}` — `DELETE` deleteResponse, `GET` getResponse
- [ ] `/responses/{response_id}/input_items` — `GET` listInputItems

### Vector Stores

- [ ] `/vector_stores` ✓ — `GET` listVectorStores, `POST` createVectorStore
- [ ] `/vector_stores/{vector_store_id}` ✓ — `DELETE` deleteVectorStore, `GET` getVectorStore, `POST` modifyVectorStore
- [ ] `/vector_stores/{vector_store_id}/file_batches` ✓ — `POST` createVectorStoreFileBatch
- [ ] `/vector_stores/{vector_store_id}/file_batches/{batch_id}` ✓ — `GET` getVectorStoreFileBatch
- [ ] `/vector_stores/{vector_store_id}/file_batches/{batch_id}/cancel` ✓ — `POST` cancelVectorStoreFileBatch
- [ ] `/vector_stores/{vector_store_id}/file_batches/{batch_id}/files` ✓ — `GET` listFilesInVectorStoreBatch
- [ ] `/vector_stores/{vector_store_id}/files` ✓ — `GET` listVectorStoreFiles, `POST` createVectorStoreFile
- [ ] `/vector_stores/{vector_store_id}/files/{file_id}` ✓ — `DELETE` deleteVectorStoreFile, `GET` getVectorStoreFile
