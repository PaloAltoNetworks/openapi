Generated by scripts/build.py from openapi.yaml.

operations                       187
paths                            121
schemas                          442
tags                             42

docs URLs                        187
  x-mint.href on every operation, from _project/hrefs.yaml. These are
  public URLs: the build re-derives each one and fails if it has moved,
  because a tag rename changes them silently and links point at them.

prose                            0 written of 417 fields
  Authored in overlays/docs-prose.yaml under the grounding gate and
  merged into openapi.yaml by this script. Every prose field here is
  stripped and reapplied on each build, so nothing reaches the
  published document without passing that gate.

servers                          1 root + 69 path override(s)
  Every URL comes from _project/servers.yaml; which plane a path is on
  comes from _project/planes.yaml. Edit a host there and rebuild. The
  first entry in a plane is the one Mintlify builds its sample from;
  the rest appear in its base-URL dropdown. The gateway is the root
  block, so its paths carry no override.
    gateway          52 path(s)
      https://aigw.portkey.ai/v1  -- Managed
      https://self-hosted-gateway-url.example.com/v1  -- Self-hosted -- replace with your own gateway host
    control-plane    51 path(s)
      https://api.apps.paloaltonetworks.com/ai_gw/v2  -- Managed
    admin            14 path(s)
      https://api.apps.paloaltonetworks.com/ai_gw/admin/v2  -- Managed
    admin-in-path     4 path(s)
      https://api.apps.paloaltonetworks.com/ai_gw  -- Managed

security                         1 scheme
  One Authorization bearer token, declared once at the root. Six schemes
  in five combinations were collapsed into it.
    operation-level overrides removed this run  0

operations requiring no authentication  1
  Inherited `security: []`. Preserved rather than quietly reversed, but
  each is a claim that the endpoint is public and wants confirming.
    GET /model-configs/pricing/{provider}/{model}

components pruned this run       0
  Reachability sweep. Zero on a clean tree; it earns its place the next
  time a group of operations is dropped.

null defaults dropped this run   0

operations lacking operationId   45
  Inherited gaps, not introduced here. operationId values are never
  renamed, and inventing them is not the same as recovering them, so
  they are reported for engineering rather than filled in. Phase 2.
    DELETE /api-keys/{id}
    DELETE /integrations/{slug}
    DELETE /integrations/{slug}/models
    DELETE /providers/{slug}
    GET /analytics/graphs/cache/hit-rate
    GET /analytics/graphs/cache/latency
    GET /analytics/graphs/cost
    GET /analytics/graphs/errors
    GET /analytics/graphs/errors/rate
    GET /analytics/graphs/errors/stacks
    GET /analytics/graphs/errors/status-codes
    GET /analytics/graphs/feedbacks
    GET /analytics/graphs/feedbacks/ai-models
    GET /analytics/graphs/feedbacks/scores
    GET /analytics/graphs/feedbacks/weighted
    GET /analytics/graphs/latency
    GET /analytics/graphs/requests
    GET /analytics/graphs/requests/rescued
    GET /analytics/graphs/tokens
    GET /analytics/graphs/users
    GET /analytics/graphs/users/requests
    GET /analytics/groups/ai-models
    GET /analytics/groups/metadata/{metadataKey}
    GET /analytics/groups/provider
    GET /analytics/groups/users
    GET /analytics/summary/cache
    GET /api-keys
    GET /api-keys/{id}
    GET /integrations
    GET /integrations/{slug}
    GET /integrations/{slug}/models
    GET /integrations/{slug}/workspaces
    GET /logs/{logId}
    GET /providers
    GET /providers/{slug}
    POST /api-keys/{id}/rotate
    POST /api-keys/{sub-type}
    POST /integrations
    POST /logs
    POST /providers
    PUT /api-keys/{id}
    PUT /integrations/{slug}
    PUT /integrations/{slug}/models
    PUT /integrations/{slug}/workspaces
    PUT /providers/{slug}

not shipped
  Declared in _project/drops.yaml and enforced by this script: if one
  reappears in openapi.yaml the build fails.
  tags:
    Audit Logs
    Collections
    Labels
    Log Exports
    Prompt Partials
    Prompts
    User Invites
    Users
    Virtual Keys
    Workspaces
    Workspaces > Members
  parameters, on any operation:
    organisation_id
